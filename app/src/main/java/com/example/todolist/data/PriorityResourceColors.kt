package com.example.todolist.data

import android.graphics.Color

data class PriorityResourceColors(
    val color: Int,
)

val colors = listOf(
    PriorityResourceColors(Color.RED),
    PriorityResourceColors(Color.YELLOW),
    PriorityResourceColors(Color.GREEN),
)